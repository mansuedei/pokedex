import { HighlightDamageDirective } from './highlight-damage.directive';

describe('HighlightDamageDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightDamageDirective();
    expect(directive).toBeTruthy();
  });
});
